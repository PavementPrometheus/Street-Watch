version: '3.7'
services:
 web:
  environment:
   - ENV=development # debug mode
   - DB=mongodb://mongodb:27017/test
 mongodb:
  environment:
   - MONGO_DATA_DIR=/usr/PavementApp/data/testdb
   - MONGO_LOG_DIR=/dev/null
  volumes:
   - ./PavementApp/data/testdb:/usr/PavementApp/data/testdb
